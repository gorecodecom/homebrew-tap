# GoreCode Homebrew Tap

Homebrew tap for GoreCode command-line tools.

## GoreGraph

After the first public GoreGraph release:

```bash
brew install gorecodecom/tap/goregraph
```

This repository may host additional GoreCode CLI formulae later.
